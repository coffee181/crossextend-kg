{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Semantic type constraints for relation families used by the active CrossExtend-KG runtime.",
  "propagation": {
    "description": "Causal or fault propagation edges where the head can lead to the tail.",
    "allowed_head_types": ["Fault", "Signal", "State", "Process", "Component"],
    "allowed_tail_types": ["Fault", "Signal", "State"],
    "examples": [
      "thermal runaway causes fire -> Fault -> Fault",
      "voltage drop triggers alarm -> Signal -> Fault",
      "high temperature accelerates degradation -> State -> Fault",
      "fast charging induces lithium plating -> Process -> Fault"
    ],
    "semantic_rules": [
      "The head must be able to act as a causal source.",
      "The tail must be a resulting fault, signal, or state.",
      "Fault -> Fault captures fault chains.",
      "Component -> Fault captures component-induced failures."
    ]
  },
  "structural": {
    "description": "Physical containment or part-of hierarchy edges.",
    "allowed_head_types": ["Asset", "Component"],
    "allowed_tail_types": ["Asset", "Component"],
    "examples": [
      "battery pack contains cells -> Asset -> Component",
      "sensor belongs to controller -> Component -> Component",
      "module includes sensors -> Component -> Component",
      "vehicle has battery pack -> Asset -> Asset"
    ],
    "semantic_rules": [
      "Only physical entities participate in structural hierarchy.",
      "Asset is a top-level industrial asset and Component is a subassembly or part.",
      "Process, State, and Document are not structural entities."
    ]
  },
  "task_dependency": {
    "description": "Execution or task-output dependencies where the head performs, triggers, measures, or confirms the tail.",
    "allowed_head_types": ["Task", "Process", "Actor"],
    "allowed_tail_types": ["Task", "Process", "Signal", "Document", "Fault", "State", "Asset", "Component"],
    "examples": [
      "inspection confirms fault -> Task -> Fault",
      "engineer performs diagnosis -> Actor -> Process",
      "analysis produces report -> Process -> Document",
      "monitoring outputs signal -> Task -> Signal"
    ],
    "semantic_rules": [
      "The head must be able to execute or produce something.",
      "The tail may be a next task, an observed output, or a discovered condition.",
      "Component and Asset may appear as tails but not as executing heads."
    ]
  },
  "communication": {
    "description": "Diagnostic evidence or information-flow edges where an observable item indicates another concept.",
    "allowed_head_types": ["Component", "Signal", "Process", "State"],
    "allowed_tail_types": ["Signal", "State", "Component", "Asset", "Actor", "Task", "Fault"],
    "examples": [
      "sensor detects temperature -> Component -> Signal",
      "alarm notifies operator -> Signal -> Actor",
      "fresh wetting indicates connector leakage -> Signal -> Fault",
      "wet after shutdown indicates leak persistence -> State -> Fault"
    ],
    "semantic_rules": [
      "The head must be able to produce, carry, or embody diagnostic evidence.",
      "Signal and State heads are allowed when an observation or standing condition indicates another concept.",
      "Document is not a valid communication head."
    ]
  },
  "lifecycle": {
    "description": "State transition or experience edges where the head changes into or undergoes the tail.",
    "allowed_head_types": ["Asset", "Component", "Fault", "State", "MaintenanceAction"],
    "allowed_tail_types": ["State", "MaintenanceAction", "Incident", "Process"],
    "examples": [
      "battery experiences degradation -> Asset -> State",
      "fault triggers repair -> Fault -> MaintenanceAction",
      "normal transitions to warning -> State -> State",
      "device undergoes maintenance -> Asset -> Process"
    ],
    "semantic_rules": [
      "The head must be able to undergo a state or lifecycle change.",
      "The tail must be a resulting state, action, incident, or process.",
      "State -> State captures explicit state transitions.",
      "Fault -> MaintenanceAction captures maintenance triggered by a fault."
    ]
  },
  "_metadata": {
    "version": "1.1",
    "created": "2026-04-18",
    "purpose": "Provide semantic type validation for relation families during graph assembly.",
    "usage": "Any triple that violates these family constraints is exported as rejected_type."
  }
}
